SERVING WEBPAGES USING A WEB SERVER

There are many different tools (called web servers) to serve websites to the user. That is, to send files to them.

This module will cover how to set up and serve websites with the following tools:

> Express, NGINX, PM2
> Apache (pending)